{
  "installCommand": "cd stage-2 && npm install",
  "buildCommand": "cd stage-2 && npm run build",
  "outputDirectory": "stage-2/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
